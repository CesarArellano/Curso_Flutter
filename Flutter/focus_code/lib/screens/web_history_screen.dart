import 'package:flutter/material.dart';
  
class WebHistoryScreen extends StatelessWidget {
  
  const WebHistoryScreen({Key? key}) : super(key: key);
  
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('WebHistoryScreen'),
    );
  }
}